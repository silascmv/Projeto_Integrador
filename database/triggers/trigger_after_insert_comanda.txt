DELIMITER //

CREATE TRIGGER comanda_after_insert
AFTER INSERT
   ON COMANDA FOR EACH ROW

BEGIN

   DECLARE MESA varchar(50);


   -- Insert record into audit table
   UPDATE MESAS SET SN_DISPONIVEL = 0 WHERE ID_MESA = NEW.ID_MESA
   
END; //

DELIMITER ;